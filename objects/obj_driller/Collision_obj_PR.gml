/// @description Insert description here
// You can write your code in this editor



// Inherit the parent event
event_inherited();
if other.line == line 
	{
	if other.bam == 1 && digging == 1
	{	
		with (other) {
			action_kill_object();
		}
		action_create_object(obj_shutdown, other.x + 0, other.y + 40);
	}
}